import React from "react";
import { render } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";

describe("Requirements", function() {
  it("Describes the filters via JSON", () => {
    expect(true);
  });

  it("Auto generates the input fields using a theme", () => {
    expect(true);
  });

  it("Does validation and error messaging", () => {
    expect(true);
  });

  it("Filters can be grouped and nested", () => {
    expect(true);
  });
});
